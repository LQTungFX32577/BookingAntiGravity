---
description: Stages all changes, commits them with a message, and pushes to the remote repository.
---

1. Check current status
   git status

2. Stage all changes
   // turbo
   git add .

3. Commit changes
   git commit -m "Your commit message"

4. Push to remote
   git push
